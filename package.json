{
  "name": "pixelflow-styled-fof-v3",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "description": "Pixel Flow recreation — v3 styled Fish of Fortune build (juice + reskin; gameplay + core v2 timings identical).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
